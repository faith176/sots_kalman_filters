\centering
\begin{tikzpicture}[
node distance=2cm,
box/.style={draw, rectangle, rounded corners, minimum width=3cm, minimum height=1cm, align=center},
layer/.style={draw, dashed, rounded corners, inner sep=8pt}
]

% SoTS Layer
\node[box] (controller) {SoTS Controller\\(Belonging \& Health Monitoring)};

\node[box, below left=of controller] (dt1) {DT 1};
\node[box, below=of controller] (dt2) {DT 2};
\node[box, below right=of controller] (dt3) {DT 3};

\draw[->] (dt1) -- (controller);
\draw[->] (dt2) -- (controller);
\draw[->] (dt3) -- (controller);

% CEP service
\node[box, below=3cm of controller] (cep) {CEP Service};

\draw[->] (controller) -- node[right]{Filtered / Reliable Inputs} (cep);

% labels
\node[layer, fit=(controller) (dt1) (dt2) (dt3), label=above:SoTS Coordination Layer] {};
\node[layer, fit=(cep), label=below:Service Layer] {};

\end{tikzpicture}